# Copie para `.env` e ajuste apenas se precisar apontar o front para outro ambiente.
# Sem `.env`, a aplicação usa os padrões de desenvolvimento de src/environments/environment.ts.
#
# O prefixo NG_APP_ é obrigatório: só variáveis com ele são injetadas no bundle
# pelo @ngx-env/builder.
#
# ATENÇÃO: estes valores vão para o bundle e são públicos. Nunca coloque segredos
# aqui (service_role key do Supabase, senhas, tokens de API privados).

NG_APP_API_URL=http://localhost:8080
NG_APP_SUPABASE_URL=https://moorxvcnxesniwaaaksm.supabase.co
NG_APP_SUPABASE_ANON_KEY=sb_publishable_ZV_APlGr2rAAQ05nYIDZRg_4UiPGXS1
